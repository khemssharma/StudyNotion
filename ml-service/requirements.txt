# -------------------------------------------------------
# StudyNotion ML Service - Inference (Render deployment)
# Neural Collaborative Filtering (NCF) Edition
# -------------------------------------------------------

# Web framework
flask==3.0.3
flask-cors==4.0.1
gunicorn==22.0.0

# Numerical helpers (always needed)
numpy==1.26.4

# Database
pymongo==4.8.0

# PyTorch - CPU only (installed via build command with extra-index-url)
# See render.yaml buildCommand - do not add torch here directly
# torch is installed separately to avoid the +cpu suffix pip issue
